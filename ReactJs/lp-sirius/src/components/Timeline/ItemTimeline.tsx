type Props = {
    image: string;
    date: string;
    title: string;
    body: string;
}

export const ItemTimeline = ({image, date, title, body}: Props) => {
    return(
        <div className="w-full h-28 rounded-lg bg-second mt-5">

        </div>
    );
}